{
  "name": "tokenforge",
  "private": true,
  "version": "1.0.0",
  "description": "Multi-chain token launcher dApp",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@10.32.1",
  "workspaces": [
    "contracts/evm",
    "frontend",
    "shared"
  ]
}
